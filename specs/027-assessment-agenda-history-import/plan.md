# Plan

1. Add model/migration/schema for `AssessmentAppointment`.
2. Add service and router for agenda CRUD/list.
3. Add import preview/apply endpoints.
4. Include appointment coverage in assessment queue/timeline.
5. Add frontend agenda tab and import option.
6. Add tests and run focused validation.
