# Phase 4.2 Plan

## Workstreams

1. Padronizar `commit=False` e commit no router para tasks, assessments e follow-up CRM.
2. Ajustar matriz de permissao do frontend para recepcao, comercial e trainer.
3. Implementar task-lite do trainer em `Assessments`, com update de status e sem criacao/delecao.
4. Completar CI frontend com `lint` e `test`.

## Acceptance

- recepcao enxerga CRM em leitura apenas.
- comercial acessa contexto de assessment sem fila/mutacao ampla.
- trainer atualiza tarefas tecnicas em `Assessments`.
- `GET /tasks` e `PATCH /tasks/{id}` aceitam `trainer` apenas no recorte tecnico.
- CI frontend roda typecheck, lint, unit tests, build e e2e.
