# 09.7 - Validacao

## Validacoes planejadas

- `specify check`.
- Testes backend focados em WhatsApp agent.
- JSON parse de todos os workflows em `/n8n/workflows`.
- Busca por tokens/chaves reais nos artefatos `/n8n`.
- Validacao SDK dos workflows atualizados no n8n.
- Criacao/atualizacao de drafts inativos no n8n.
- Pin data para router externo seguro.
- Pin data para externo sensivel -> handoff.
- Pin data para interno baixo risco.
- Pin data para interno sensivel -> `pending_approval`.
- Teste de `07-tool-task-manager`.
- Teste de `08-tool-whatsapp-communication` bloqueando broadcast.
- Teste de `06-tool-database-query` bloqueando SQL destrutivo.

## Resultado

- `specify check`: aprovado.
- Backend: `python -m pytest tests/test_whatsapp_agent_service.py tests/test_whatsapp_router.py` aprovado, 11 testes.
- JSON parse de `n8n/workflows/*.json`: aprovado.
- Todos os workflows locais estao com `active: false`.
- Busca por segredos em `/n8n` e `saas-backend/.env.example`: sem matches reais.
- Drafts n8n atualizados/criados como inativos:
  - Router `8eApeMUW2PftRzld`.
  - Internal Operator Agent `3706DcqiJJxZQA30`.
  - External Service Agent `QRtfLjW8sgQq342D`.
  - Send Reply via Backend `kvoELJVfVxKZPM3S`.
  - WhatsApp Communication Policy `1Hf0Vz6ckBEe1NtN`.
  - Human Approval Gateway `QL3C5XqSaF4GvL4t`.
- Router externo seguro: execution `8`, retornou `send_reply`, `risk=low`.
- Router externo sensivel: execution `9`, retornou `handoff`, `risk=medium`.
- Interno baixo risco: execution `10`, retornou `send_reply`, `risk=low`.
- Interno sensivel: execution `11`, retornou `pending_approval`, `risk=high`.
- WhatsApp broadcast bloqueado: execution `12`, retornou `error`.
- Task Manager sandbox: execution `13`, retornou `success` com task.
- DB Query destrutivo: execution `14`, retornou `pending_approval`.

## Pendencias

- Configurar credenciais reais no n8n: OpenAI, Postgres e Backend Agent Service.
- Aplicar `n8n/sql/schema.sql` em Postgres real.
- Executar UAT end-to-end com WhatsApp sandbox.
- Ativar producao somente apos aprovacao humana.
