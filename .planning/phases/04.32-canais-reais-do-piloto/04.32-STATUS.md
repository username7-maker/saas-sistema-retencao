# 04.32 Status

## Contexto

Fase urgente do piloto para fechar os canais reais que ainda geravam friccao entre a promessa do produto e o comportamento em producao.

Escopo imediato desta rodada:

- avaliacao assistida
- exportacao Actuar
- WhatsApp

## O que foi confirmado

### Validacao real com Erick Bedin

- Membro usado para a validacao controlada:
  - `member_id = 42e8d19a-b65b-4011-bb61-716cfff3204f`
  - `full_name = Erick Bedin`
  - `phone = 54996316730`
  - `email = erickbedin904@gmail.com`

#### WhatsApp real para Erick Bedin

- Com a instancia oficial do piloto ja conectada, foi enviado um WhatsApp real apenas para o Erick Bedin.
- Evidencia:
  - endpoint `POST /api/v1/automations/whatsapp/send`
  - `status = 200`
  - message log `650854ba-c475-42d5-9f00-ef38940f6a56`
  - `recipient = 5554996316730`
  - `status = sent`
  - `instance_used = gym_2b11fe2e95ed4ca79a19d6cfaf855c72`

Leitura honesta:

- o canal oficial do piloto nao apenas conectou; ele tambem enviou mensagem real com sucesso
- a validacao foi feita no aluno pedido, sem usar outro membro qualquer

#### Actuar para Erick Bedin

- A avaliacao do Erick usada para a validacao controlada:
  - `evaluation_id = dd1ce930-6570-49dd-80a8-f0e4e352043a`
- O retry do sync foi executado no ambiente publicado.
- Estado final observado:
  - `sync_mode = csv_export`
  - `sync_status = manual_sync_required`
  - `external_id = csv-export:dd1ce930-6570-49dd-80a8-f0e4e352043a`
  - `last_error_code = csv_export_ready`
  - `last_error = Exportacao CSV pronta para lancamento manual no Actuar.`
- O resumo manual retornado para o Erick ficou disponivel no endpoint de resumo:
  - `evaluation_date = 2026-03-26`
  - `weight = 85.0`
  - `body_fat_percent = 23.0`
  - `lean_mass_kg = 65.0`
  - `muscle_mass_kg = 35.6`
  - `bmi = 23.0`
- Campos fora do suporte do Actuar seguiram expostos corretamente:
  - `bmr_kcal`
  - `visceral_fat`

Leitura honesta:

- o fluxo Actuar do piloto esta operacional no modo `csv_export`
- ele ainda nao e `sync automatico completo no Actuar`, mas o bridge/manual export esta funcional e auditavel

### Avaliacao assistida

- O backend publicado ja salva bioimpedancia com suporte assistido de verdade no fluxo atual.
- Quando Claude esta indisponivel por billing, o sistema cai para fallback deterministico e ainda preenche:
  - `ai_coach_summary`
  - `ai_member_friendly_summary`
  - `ai_risk_flags_json`
  - `ai_training_focus_json`
- Evidencia do piloto:
  - avaliacao `dd1ce930-6570-49dd-80a8-f0e4e352043a`
  - `source = ocr_receipt`
  - `parsed_from_image = true`
  - `ocr_confidence = 0.24`
  - summaries e focos preenchidos

Leitura honesta:

- a avaliacao assistida esta funcional no piloto, mas hoje opera em fallback porque a conta Anthropic do ambiente esta sem credito
- isso nao quebra o fluxo, mas impede a camada premium de IA

### Migracao da bioimpedancia assistida para OpenAI

- O codigo local da fase 4.32 agora prefere OpenAI somente na cadeia de bioimpedancia assistida:
  - leitura assistida por imagem
  - narrativa estruturada para coach/aluno
- Claude foi mantido apenas como fallback legado de transicao para outros ambientes, sem bloquear o piloto.
- O contrato novo esta exposto em:
  - `OPENAI_API_KEY`
  - `OPENAI_MODEL`
  - `OPENAI_VISION_MODEL`
  - `OPENAI_TIMEOUT_SECONDS`
- Cobertura local validada:
  - `31 passed` na trilha de bioimpedancia e sync

Leitura honesta:

- a substituicao para OpenAI esta pronta no codigo
- a `OPENAI_API_KEY` ja foi registrada no Railway para API e worker
- os deploys mais recentes desta rodada foram publicados
- o worker novo esta saudavel e executando `scheduler` e `actuar_sync_queue`
- a quota da OpenAI foi regularizada e a trilha premium passou a responder em producao

### OpenAI premium validada com Erick Bedin

- A prova de vida da OpenAI foi executada com chamada direta e retornou `200`.
- A narrativa assistida do Erick foi regenerada no ambiente publicado.
- Evidencia:
  - avaliacao `dd1ce930-6570-49dd-80a8-f0e4e352043a`
  - `ai_generated_at = 2026-03-26T23:54:10.175796Z`
  - `POST https://api.openai.com/v1/responses` -> `200`
- O texto novo deixou de ser o fallback curto e passou a vir com leitura mais rica e contextualizada.

Leitura honesta:

- a avaliacao assistida premium voltou a funcionar na producao do piloto
- o fallback deterministico segue como rede de seguranca, mas nao e mais o caminho principal

### Leitura assistida por imagem validada com a foto real do Erick

- A imagem original do Erick foi reenviada ao endpoint publicado:
  - `C:\\Users\\asteriscos\\Downloads\\WhatsApp Image 2026-03-24 at 15.38.14.jpeg`
- O endpoint respondeu `200` no ambiente publicado.
- Evidencia:
  - endpoint `POST /api/v1/members/42e8d19a-b65b-4011-bb61-716cfff3204f/body-composition/parse-image`
  - `engine = ai_fallback`
  - `fallback_used = true`
  - `confidence = 0.81`
  - valores centrais confirmados:
    - `weight_kg = 84.5`
    - `body_fat_kg = 19.46`
    - `body_fat_percent = 23.0`
    - `waist_hip_ratio = 0.88`
    - `muscle_mass_kg = 37.2`
    - `bmi = 26.7`
- O bug que impedia essa rota foi corrigido:
  - antes: `400 invalid_json_schema` na OpenAI
  - agora: `POST https://api.openai.com/v1/chat/completions` -> `200`

Leitura honesta:

- a leitura assistida por imagem esta funcional novamente com OpenAI
- o comportamento real ficou correto e o contrato semantico tambem foi ajustado

### Semantica do engine ajustada

- O contrato do parse assistido agora diferencia melhor os caminhos:
  - `local`
  - `hybrid`
  - `ai_assisted`
- O caminho de IA pura deixou de voltar como `ai_fallback`.
- O flag `fallback_used` agora fica `false` no fluxo puro de OpenAI e `true` apenas quando a leitura final realmente combinou ou precisou do fallback.

Evidencia em producao:

- revalidacao com a foto real do Erick depois do deploy:
  - `POST /api/v1/members/42e8d19a-b65b-4011-bb61-716cfff3204f/body-composition/parse-image` -> `200`
  - `engine = ai_assisted`
  - `fallback_used = false`
  - `confidence = 0.84`

Leitura honesta:

- agora a API esta mais fiel ao que realmente aconteceu na leitura
- a UI publicada tambem passou a refletir essa distincao sem quebrar historico antigo que ainda use `ai_fallback`

### Exportacao Actuar

- O codigo local da 4.31/4.32 implementa `csv_export` corretamente.
- O worker publicado ainda esta rodando codigo antigo, porque os deploys recentes do Railway para API/worker continuam marcados como `FAILED`.
- Para nao travar o piloto, foi executado um bridge one-off com o codigo novo contra o ambiente de producao.

Evidencia do bridge:

- avaliacao `36bc9ada-6d7b-4e48-a5d5-62f62eeefecf`
- status final da avaliacao:
  - `manual_sync_required`
  - `sync_last_error_code = csv_export_ready`
  - `sync_last_error_message = Exportacao CSV pronta para lancamento manual no Actuar.`
  - `actuar_external_id = csv-export:36bc9ada-6d7b-4e48-a5d5-62f62eeefecf`

Leitura honesta:

- a exportacao Actuar esta funcional agora em modo bridge/manual
- o estado final correto esta chegando no banco do piloto
- ainda falta colocar esse mesmo comportamento no worker publicado sem depender do bridge

### Actuar mais automatico publicado

- O piloto agora possui configuracao dedicada de Actuar por academia.
- Foi publicada uma nova API de settings:
  - `GET /api/v1/settings/actuar`
  - `PUT /api/v1/settings/actuar`
  - `POST /api/v1/settings/actuar/test-connection`
- A tela de `Settings` agora expoe uma aba `Actuar` para configurar:
  - URL base
  - usuario
  - senha
  - habilitacao do conector
  - auto-sync de bioimpedancia
- O backend agora resolve o modo efetivo de sync assim:
  - se nao houver credenciais validas, o piloto fica em `csv_export`
  - se houver credenciais validas, o fluxo sobe para `assisted_rpa`
- O runtime do backend/worker foi preparado com `playwright` para sustentar a trilha automatizada.

Evidencia em producao:

- `GET /api/v1/settings/actuar` -> `200`
- estado atual do piloto:
  - `actuar_enabled = true`
  - `actuar_auto_sync_body_composition = true`
  - `actuar_base_url = null`
  - `actuar_username = null`
  - `actuar_has_password = false`
  - `environment_enabled = true`
  - `environment_sync_mode = csv_export`
  - `effective_sync_mode = csv_export`
  - `automatic_sync_ready = false`
- `POST /api/v1/settings/actuar/test-connection` -> `200`
- resposta atual:
  - `success = false`
  - `provider = actuar_assisted_rpa`
  - `effective_sync_mode = csv_export`
  - `automatic_sync_ready = false`
  - mensagem: `Configure URL, usuario e senha do Actuar para ativar o modo automatico.`

Leitura honesta:

- o piloto ja nao depende so de `csv_export/manual` como arquitetura
- o caminho mais automatico ja esta no ar e configuravel
- o que ainda impede o `assisted_rpa` real hoje e apenas a ausencia de credenciais validas do Actuar na academia piloto

### WhatsApp

- A Evolution API foi hospedada publicamente de forma isolada no Railway.
- O backend do piloto foi reimplantado com as variaveis `WHATSAPP_*` carregadas.
- O worker do piloto tambem foi reimplantado com sucesso para manter a integracao consistente.
- O fluxo de conexao real agora responde no piloto e cria a instancia da academia.

Evidencia:

- Evolution publica:
  - `https://evolution-api-production-0205.up.railway.app`
- hardening aplicado:
  - docs desabilitadas
  - manager desabilitado
  - autenticacao por API key forte
  - webhook token forte separado
  - volume persistente em `/evolution/instances`
- deploys de recarga concluidos:
  - API `2736951e-c1b6-4e1c-9a02-b0e1f88b6c34` -> `SUCCESS`
  - worker `af284015-14fb-4642-8056-7d6652cbd9c4` -> `SUCCESS`
- smoke do piloto:
  - `POST /api/v1/whatsapp/connect` -> `200`
  - resposta inicial: `status = connecting`
  - `GET /api/v1/whatsapp/status` -> `200`
  - status atual: `connected`
  - phone atual: `555496316730`
  - instance atual: `gym_2b11fe2e95ed4ca79a19d6cfaf855c72`

Leitura honesta:

- o bloqueio de infraestrutura/configuracao foi resolvido
- o canal ja esta operacional do lado do sistema
- o pareamento de campo foi concluido com sucesso no piloto

### Evolution API local em Docker

- Uma stack local foi instalada em `c:\aigymos\evolution-docker`.
- A API local ficou funcional em `http://localhost:8088`.
- A autenticacao da API local foi configurada com `AUTHENTICATION_API_KEY` salva no `.env` da stack.
- Validacao local:
  - `GET /` -> `200`
  - `GET /instance/fetchInstances` autenticado -> `200`

Leitura honesta:

- a API local da Evolution esta pronta para testes
- isso ainda nao fecha a integracao com o backend do piloto no Railway, porque o Railway precisa enxergar uma URL publica, nao `localhost`
- o manager web oficial da Evolution veio com bug na imagem (`nginx.conf`) e foi desligado; a API segue saudavel

## Estado atual

### Resolvido

- avaliacao assistida segue operavel no piloto, com fallback deterministico
- avaliacao assistida premium via OpenAI validada em producao
- leitura assistida por imagem via OpenAI validada com a foto real do Erick
- exportacao Actuar ja consegue chegar ao estado `csv_export_ready`
- Evolution publica hospedada com seguranca e integrada ao piloto
- instancia WhatsApp da academia piloto criada e conectada com sucesso
- envio real de WhatsApp validado com Erick Bedin
- exportacao Actuar validada com Erick Bedin em modo `csv_export`

### Parcial

- exportacao Actuar ainda depende de credenciais reais para sair de `csv_export` e entrar em `assisted_rpa`

### Bloqueado externamente

- Claude premium: sem credito na conta Anthropic do ambiente
- Actuar automatico: faltam `base_url`, `usuario` e `senha` reais da academia piloto
- nenhum bloqueio externo restante para o canal WhatsApp do piloto

## Proximo passo recomendado

1. Testar um envio real de WhatsApp no piloto
2. Preencher as credenciais reais do Actuar em `Settings > Actuar` e validar o `test-connection`
3. Repor credito da conta Anthropic apenas se ainda houver interesse em manter Claude vivo como fallback legado
4. Trocar/rotacionar a chave OpenAI exposta na conversa antes de uso definitivo
