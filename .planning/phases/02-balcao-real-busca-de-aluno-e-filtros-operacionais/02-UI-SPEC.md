# Phase 2 UI Spec

## Members Search

- Placeholder: `Buscar por nome, email ou matricula...`
- Busca continua inline no topo da tela; nao cria nova interface.

## Operational Filters

- `Sem check-in`: `Qualquer atividade`, `7+ dias`, `14+ dias`, `30+ dias`
- `Provisorios`: `Todos`, `Apenas provisorios`, `Ocultar provisorios`

## Row Visuals

- Badge de `Matricula {external_id}` quando existir.
- Badge `Provisorio` quando o membro vier marcado assim no `extra_data`.
