# Phase 7.3 Validation - BI Foundation Upgrade

## Validacao automatizada

- Backend: `test_dashboard_service.py` cobre composicao do payload e flags de base ausente.
- Frontend: `DashboardLovable.test.tsx` cobre renderizacao da nova leitura de execucao/gestao.

## Validacao manual pendente

1. Abrir Dashboard Executivo como owner/manager.
2. Verificar se `BI foundation` mostra onboarding, execucao e Autopilot sem poluir a tela.
3. Abrir Relatorios e confirmar se `Base de BI` traz execucao 7d, Autopilot e proxima acao de gestao.
4. Validar com gestor se as acoes gerenciais sao acionaveis.

## Criterio de aceite

Gestor entende em menos de 30 segundos:

- quanto de receita esta em risco;
- se onboarding esta ativando;
- se a fila operacional esta acumulando atraso;
- se Autopilot esta resolvendo ou escalando;
- qual decisao gerencial tomar primeiro.
