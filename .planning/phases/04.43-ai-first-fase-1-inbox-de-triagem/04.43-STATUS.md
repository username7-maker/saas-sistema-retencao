# 04.43 Status

## Estado

- `execute`: concluido
- `verify/validate`: concluido
- corte atual: `Wave 4 - Pilot evidence and baseline comparison`
- data: `2026-04-19`

## O que entrou

- modelo persistido `AITriageRecommendation`
- migration `20260418_0031_add_ai_triage_recommendations.py`
- schema canônico `AITriageRecommendationRead`
- servico de agregacao `retention + onboarding`
- rotas:
  - `GET /api/v1/ai/triage/items`
  - `GET /api/v1/ai/triage/items/{recommendation_id}`
  - `PATCH /api/v1/ai/triage/items/{recommendation_id}/approval`
  - `POST /api/v1/ai/triage/items/{recommendation_id}/actions/prepare`
  - `PATCH /api/v1/ai/triage/items/{recommendation_id}/outcome`
  - `GET /api/v1/ai/triage/metrics/summary`
- auditoria tenant-safe para:
  - `ai_triage_recommendation_suggested`
  - `ai_triage_recommendation_refreshed`
  - `ai_triage_list_viewed`
  - `ai_triage_detail_viewed`
  - `ai_triage_recommendation_approved`
  - `ai_triage_recommendation_rejected`
  - `ai_triage_action_prepared`
  - `ai_triage_action_unavailable`
  - `ai_triage_outcome_updated`
- frontend dedicado:
  - rota `/ai/triage`
  - shell AI-first na navegacao principal
  - lista priorizada com inbox dedicada
  - inspector lateral com explainability
  - estados `loading` / `empty` / `error` / `degraded`
  - aprovacao humana item por item
  - tool layer segura para:
    - criar task
    - atribuir owner
    - abrir follow-up
    - preparar mensagem
    - explicitar job aprovado como estado honesto quando nao houver contrato seguro
  - bloco de outcome observado
- KPI strip com aceitacao, tempo medio de aprovacao e acoes preparadas
- walkthrough real no piloto com sample controlado:
  - `Wave 4 Retention Member`
  - `Wave 4 Onboarding Member`
- evidencias reais da Wave 4:
  - captura browser do inbox/lista/detalhe/approved/metrics
  - comparacao formal contra o baseline congelado
  - `04.43-VALIDATION.md` com `PASS`

## Leitura do corte

- a inbox AI-first nao abriu uma arquitetura paralela
- retencao continua reutilizando `get_retention_queue`
- onboarding continua reutilizando os sinais reais ja presentes em `Member`, `Assessment` e `Task`
- as recommendations agora ficam persistidas como snapshot semanticamente estavel para aprovacao, preparacao segura e outcome observado
- a Wave 3 ficou ancorada em `Task` + `AuditLog`, sem abrir um segundo motor de execucao
- `enqueue_approved_job` ja existe no contrato da tool layer, mas hoje responde de forma honesta como indisponivel quando nao ha job seguro mapeado para aquela recommendation

## Validacao

- `pytest -q tests/test_ai_triage_service.py tests/test_ai_triage_router.py`
- resultado: `16 passed`
- `npm run test -- --run src/test/AITriageInboxPage.test.tsx`
- resultado: `4 passed`
- `npm run build`
- resultado: `ok`
- `specify check`
- resultado: `ok`

## Fechamento

- a `Wave 4` foi validada no piloto
- a `4.43` agora tem backend, UI, tool layer segura, outcome measurement e evidencia real de walkthrough
- o proximo movimento deixou de ser "provar a primeira inbox AI-first" e virou reduzir o atrito operacional da superficie validada
- esse corte incremental agora esta ancorado na `4.43.1 - simplificacao operacional do AI Inbox`
