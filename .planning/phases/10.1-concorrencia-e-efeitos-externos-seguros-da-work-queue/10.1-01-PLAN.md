---
phase: 10.1-concorrencia-e-efeitos-externos-seguros-da-work-queue
plan: "01"
type: execute
wave: 1
depends_on:
  - "10-03"
files_modified:
  - saas-backend/app/models/task.py
  - saas-backend/app/models/work_queue_claim.py
  - saas-backend/app/models/__init__.py
  - saas-backend/app/schemas/task.py
  - saas-backend/app/schemas/work_queue.py
  - saas-backend/app/routers/work_queue.py
  - saas-backend/app/services/work_queue_service.py
  - saas-backend/app/services/ai_triage_service.py
  - saas-backend/alembic/versions/20260713_0050_work_queue_concurrency_safe_effects.py
  - saas-backend/tests/test_ai_triage_service.py
  - saas-backend/tests/test_work_queue_service.py
  - saas-backend/tests/test_work_queue_router.py
autonomous: true
requirements:
  - WQ-06
  - WQ-09
must_haves:
  truths:
    - "Task ativa equivalente possui work_dedupe_key persistente e unique parcial tenant-scoped."
    - "execute/outcome/send-and-wait aceitam expected_version e retornam 409 tipado quando a versao mudou."
    - "WorkQueueItem expoe claim_version/claimed_by para que a UI tenha precondicao observavel."
---

<objective>
Implementar dedupe canonica de Task ativa, claim sidecar e CAS 409 para mutacoes da Work Queue.

Boundary: nao mexer em provider real, consentimento ou dispatch duravel neste plano; isso fica no 10.1-02. Nao criar tabela `work_items`.
</objective>

<tasks>
<task id="10.1-01-01" type="auto" tdd="true">
  <name>Wave 0 - contratos RED de expected_version, claim e dedupe</name>
  <read_first>
    saas-backend/tests/test_work_queue_service.py
    saas-backend/tests/test_work_queue_router.py
    saas-backend/app/schemas/work_queue.py
    saas-backend/app/services/work_queue_service.py
  </read_first>
  <action>
Adicionar testes sinteticos que falham antes da implementacao: `WorkQueueExecuteInput(expected_version=1)`, `WorkQueueOutcomeInput(expected_version=1)`, payload HTTP encaminhando `expected_version`, conflito 409 contendo `code=work_queue_conflict`, `current_version`, `claimed_by_user_id`, `claimed_by_name`, e reuso de task ativa quando `IntegrityError` ocorre na chave canonica.
  </action>
  <acceptance_criteria>
    - Testes contem os termos `expected_version`, `work_queue_conflict` e `work_dedupe_key`.
    - RED falha por campo/funcao ausente, nao por import quebrado.
  </acceptance_criteria>
</task>

<task id="10.1-01-02" type="auto">
  <name>Modelo e migration aditiva</name>
  <read_first>
    saas-backend/app/models/task.py
    saas-backend/app/models/autopilot.py
    saas-backend/app/models/__init__.py
    saas-backend/alembic/versions/20260707_0049_anthropometry_protocols_body_map.py
  </read_first>
  <action>
Adicionar `Task.work_dedupe_key` nullable/indexado e novo modelo `WorkQueueClaim` com unique `(gym_id, source_type, source_id)`, `version`, claimant e metadata. Criar migration `20260713_0050_work_queue_concurrency_safe_effects.py` com coluna, tabela, indices e unique parcial `uq_tasks_active_work_dedupe`.
  </action>
  <acceptance_criteria>
    - `task.py` contem `work_dedupe_key`.
    - `work_queue_claim.py` contem `UniqueConstraint("gym_id", "source_type", "source_id"`.
    - Migration revisa `20260707_0049` e contem `uq_tasks_active_work_dedupe`.
  </acceptance_criteria>
</task>

<task id="10.1-01-03" type="auto">
  <name>CAS em execute/outcome e reuso canonico</name>
  <read_first>
    saas-backend/app/services/work_queue_service.py
    saas-backend/app/services/ai_triage_service.py
    saas-backend/app/schemas/work_queue.py
    saas-backend/app/routers/work_queue.py
  </read_first>
  <action>
Adicionar campos de claim no `WorkQueueItemOut`; adicionar `expected_version` aos inputs de execute/outcome/send-and-wait. Implementar helpers `_get_or_create_work_queue_claim`, `_assert_work_queue_version`, `_advance_work_queue_claim`, `_work_queue_conflict`. Chamar CAS antes de mutar task/autopilot/recommendation e apos a mutacao registrar nova versao. Em AI triage, calcular `work_dedupe_key` na criacao/reuso de task e tratar `IntegrityError` como recuperacao da vencedora.
  </action>
  <acceptance_criteria>
    - Mutacoes com expected_version obsoleto retornam HTTP 409 e nao criam `TaskEvent` novo.
    - Mutacoes sem expected_version ainda funcionam para compatibilidade, mas retornam `claim_version` atualizado.
    - Task ativa equivalente com mesma `work_dedupe_key` e tenant e reutilizada; done/cancelled/deleted/archived nao.
  </acceptance_criteria>
</task>
</tasks>

<verification>
cd saas-backend; py -3.12 -m pytest -q tests/test_work_queue_service.py tests/test_work_queue_router.py tests/test_ai_triage_service.py tests/test_ai_triage_router.py
</verification>
