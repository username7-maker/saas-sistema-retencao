---
phase: 10.1-concorrencia-e-efeitos-externos-seguros-da-work-queue
plan: "03"
type: execute
wave: 3
depends_on:
  - "10.1-01"
  - "10.1-02"
files_modified:
  - saas-backend/tests/test_work_queue_postgres_concurrency.py
  - specs/055-work-queue-concurrency-safe-effects/tasks.md
  - .planning/phases/10.1-concorrencia-e-efeitos-externos-seguros-da-work-queue/10.1-SUMMARY.md
autonomous: true
requirements:
  - WQ-06
  - WQ-07
  - WQ-09
must_haves:
  truths:
    - "PostgreSQL harness usa duas sessions independentes e nao MagicMock."
    - "Provider sintetico prova zero/uma chamada e estado incerto sem retry."
    - "Spec Kit 055 registra tarefas concluidas e limites nao publicados."
---

<objective>
Criar prova final de concorrencia/provider e fechar rastreabilidade GSD/Spec Kit sem deploy automatico.
</objective>

<tasks>
<task id="10.1-03-01" type="auto">
  <name>Harness PostgreSQL real</name>
  <read_first>
    saas-backend/tests/conftest.py
    saas-backend/app/database.py
    saas-backend/app/models/task.py
    saas-backend/app/models/work_queue_claim.py
  </read_first>
  <action>
Criar `tests/test_work_queue_postgres_concurrency.py` que le `WORK_QUEUE_TEST_DATABASE_URL` ou `TEST_DATABASE_URL`. Se ausente, usar `pytest.skip("PostgreSQL concurrency harness requires ...")`. Quando presente, criar schema/fixtures sinteticos, duas sessions independentes e provar: dedupe canonica gera uma task ativa; claim/outcome tem um vencedor e um 409; TaskEvent nao duplica.
  </action>
  <acceptance_criteria>
    - O arquivo nao importa MagicMock.
    - O skip e explicito quando nao houver URL.
    - Com PostgreSQL real, dois sessions independentes sao criados.
  </acceptance_criteria>
</task>

<task id="10.1-03-02" type="auto">
  <name>Provider sintetico e estado incerto</name>
  <read_first>
    saas-backend/tests/test_autopilot_services.py
    saas-backend/app/services/autopilot_action_service.py
    saas-backend/app/services/work_queue_service.py
  </read_first>
  <action>
Adicionar casos de provider sintetico para `sent`, `failed` e `uncertain`. Provar que replay de `uncertain` nao chama provider novamente e retorna action duravel com detalhe recuperavel.
  </action>
  <acceptance_criteria>
    - Contador de provider fica `1` em replay de sucesso/incerto.
    - Bloqueio de consentimento mantem contador `0`.
  </acceptance_criteria>
</task>

<task id="10.1-03-03" type="auto">
  <name>Full gate e closeout documental</name>
  <read_first>
    specs/055-work-queue-concurrency-safe-effects/tasks.md
    .planning/phases/10.1-concorrencia-e-efeitos-externos-seguros-da-work-queue/10.1-VALIDATION.md
  </read_first>
  <action>
Rodar gates focados, atualizar `specs/055.../tasks.md`, criar `10.1-SUMMARY.md` com comandos/exit codes e declarar explicitamente se PostgreSQL real rodou ou foi skip por falta de URL. Nao fazer deploy.
  </action>
  <acceptance_criteria>
    - Summary contem backend, frontend, PostgreSQL harness, build/lint/spec/diff.
    - Summary nao afirma piloto publicado validado.
    - Worktree termina limpo.
  </acceptance_criteria>
</task>
</tasks>

<verification>
cd saas-backend; py -3.12 -m pytest -q tests/test_work_queue_service.py tests/test_work_queue_router.py tests/test_ai_triage_service.py tests/test_ai_triage_router.py tests/test_autopilot_services.py
cd saas-backend; py -3.12 -m pytest -q tests/test_work_queue_postgres_concurrency.py
cd saas-frontend; npm.cmd test -- --run src/test/WorkExecutionView.test.tsx src/test/AITriageInboxPage.test.tsx src/test/TasksPage.test.tsx
cd saas-frontend; npm.cmd run build
cd saas-frontend; npm.cmd run lint
specify check
git diff --check
</verification>

