---
phase: 10-integridade-operacional-da-fila-de-tasks-do-piloto
plan: "02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - saas-backend/app/schemas/ai_triage.py
  - saas-backend/app/schemas/work_queue.py
  - saas-backend/app/services/ai_triage_service.py
  - saas-backend/app/services/work_queue_service.py
  - saas-backend/tests/test_ai_triage_service.py
  - saas-backend/tests/test_work_queue_service.py
autonomous: true
requirements:
  - WQ-03
  - WQ-04
must_haves:
  truths:
    - "Preparar sequencialmente a mesma recomendacao ativa converge para a mesma task e nao aumenta a quantidade de tasks."
    - "Uma task encerrada, cancelada, deletada, arquivada ou de outro tenant nunca e reutilizada."
    - "A recomendacao aponta para a task ativa canonica e oferece o estado necessario para o CTA Continuar tarefa."
    - "Frescor, assigned_to_name/assigned_to_role, prazo, motivo e sinais ausentes ficam explicitos; ausencia nunca vira criticidade inventada."
  artifacts:
    - path: "saas-backend/app/services/ai_triage_service.py"
      provides: "Reuso sequencial tenant-scoped de task ativa equivalente"
      contains: "payload_snapshot"
    - path: "saas-backend/app/schemas/work_queue.py"
      provides: "Campos aditivos de task canonica, frescor e prontidao"
      contains: "freshness_state"
    - path: "saas-backend/tests/test_ai_triage_service.py"
      provides: "Matriz de reuso, exclusoes e repeticao sequencial"
  key_links:
    - from: "saas-backend/app/services/ai_triage_service.py"
      to: "Task"
      via: "gym_id + member_id + dominio/source de onboarding + estado ativo"
      pattern: "work_queue_equivalence_key"
    - from: "saas-backend/app/services/work_queue_service.py"
      to: "saas-backend/app/schemas/work_queue.py"
      via: "serializacao de canonical_task_id/freshness/readiness"
      pattern: "canonical_task_id"
---

<objective>
Eliminar criacao sequencial artificial de tasks de onboarding e tornar a recomendacao operacionalmente legivel.

Purpose: Fechar WQ-03 no limite sequencial permitido neste P0 e WQ-04 com task canonica, frescor e lacunas explicitas.
Output: Reuso tenant-scoped sem migration, payload de prontidao aditivo e regressao backend.

Boundary: Este plano nao promete concorrencia. Unique constraint, migration, lock, claim, version/CAS, 409 concorrente, WQ-06, WQ-07, WQ-09, consentimento, intent idempotente e provider pertencem a Phase 10.1 e sao proibidos aqui.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-CONTEXT.md
@.planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-PRD.md
@.planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-RESEARCH.md
@.planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-VALIDATION.md
@specs/054-work-queue-integrity-p0/spec.md
@.planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-01-SUMMARY.md
</context>

<tasks>

<task id="10-02-01" type="auto" tdd="true">
  <name>Task 1: Wave 0 - congelar reuso ativo e prontidao desconhecida</name>
  <read_first>
    saas-backend/tests/test_ai_triage_service.py
    saas-backend/tests/test_work_queue_service.py
    saas-backend/app/services/ai_triage_service.py
    saas-backend/app/services/work_queue_service.py
    saas-backend/app/schemas/ai_triage.py
    saas-backend/app/schemas/work_queue.py
    .planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-VALIDATION.md
  </read_first>
  <files>saas-backend/tests/test_ai_triage_service.py, saas-backend/tests/test_work_queue_service.py</files>
  <behavior>
    - payload_snapshot.metadata.prepared_task_id valido e ativo e reutilizado; duas preparacoes sequenciais devolvem o mesmo UUID com `create_task.call_count == 1`.
    - Sem link preparado, uma task ativa equivalente do mesmo gym/member/onboarding e localizada, vinculada e reutilizada.
    - Somente TaskStatus.TODO/DOING com deleted_at nulo, nao arquivada operacionalmente e do mesmo gym pode ser reutilizada.
    - Task canonica, freshness fresh/stale/unknown, assigned_to_name/assigned_to_role, campos faltantes e sinal unknown aparecem no WorkQueueItem sem severidade critica inferida.
  </behavior>
  <action>
Escrever primeiro testes nomeados `test_wq_reuse_*` e `test_wq_readiness_*`. Em `test_ai_triage_service.py`, usar colecao sintetica de tasks e `MagicMock` de `create_task`: executar duas preparacoes sequenciais, exigir o mesmo UUID, `create_task.call_count == 1` e `payload_snapshot.metadata.prepared_task_id` persistido pelo snapshot; cobrir ainda equivalente ativa, DONE/CANCELLED, deleted_at, arquivamento, cross-tenant e link quebrado. Em `test_work_queue_service.py`, cobrir task canonica, frescor, assignment, readiness, ausencia comprovada e zero legado. Rodar o slice pelos prefixos, aceitar RED somente quando os IDs esperados falharem por assertions de comportamento e registrar IDs/motivos no `10-02-SUMMARY.md`; erro de import/fixture/coleta nao e RED valido.
  </action>
  <acceptance_criteria>
    - Os casos possuem prefixos `test_wq_reuse_`/`test_wq_readiness_` e o RED guard reconhece somente esses IDs esperados.
    - Duas chamadas sequenciais usam MagicMock/colecao sintetica, devolvem o mesmo UUID, deixam `create_task.call_count == 1` e persistem prepared_task_id no snapshot.
    - Os testes distinguem ausencia comprovada de zero legado e verificam assignment/frescor sem criticidade inferida.
    - Nenhum teste usa duas sessions, lock, provider ou credencial real; esses casos ficam na Phase 10.1.
  </acceptance_criteria>
  <verify>
    <automated>cd saas-backend; $out = (py -3.12 -m pytest -q tests/test_ai_triage_service.py tests/test_work_queue_service.py -k "test_wq_reuse_ or test_wq_readiness_" | Out-String); $code = $LASTEXITCODE; Write-Output $out; if ($code -eq 0 -or $out -notmatch "test_wq_(reuse|readiness)_") { exit 1 } else { exit 0 }</automated>
  </verify>
  <done>A matriz Wave 0 executa RED somente nos IDs esperados, registra seus motivos e descreve a garantia sequencial e o payload de prontidao.</done>
</task>

<task id="10-02-02" type="auto" tdd="true">
  <name>Task 2: Reutilizar e persistir a task ativa canonica em preparacoes sequenciais</name>
  <read_first>
    saas-backend/app/services/ai_triage_service.py
    saas-backend/app/schemas/ai_triage.py
    saas-backend/tests/test_ai_triage_service.py
    saas-backend/app/models/task.py
    saas-backend/app/models/ai_triage_recommendation.py
    saas-backend/app/services/task_service.py
    specs/054-work-queue-integrity-p0/spec.md
  </read_first>
  <files>saas-backend/app/services/ai_triage_service.py, saas-backend/app/schemas/ai_triage.py, saas-backend/tests/test_ai_triage_service.py</files>
  <behavior>
    - A ordem de resolucao e payload_snapshot.metadata.prepared_task_id ativo, equivalence_key ativa, equivalente legado ativo e somente entao criacao.
    - A task escolhida pertence ao mesmo gym_id e member_id e representa onboarding para a mesma origem operacional.
    - O vinculo fica persistido por `_persist_snapshot` em payload_snapshot.metadata e a resposta devolve task_id/canonical_task_id.
  </behavior>
  <action>
No fluxo de `prepare`, resolver a task antes de criar: (1) ler `payload_snapshot.metadata.prepared_task_id`, carregar sob o mesmo `gym_id` e aceitar apenas task ativa; (2) buscar a chave canonica `gym_id:member_id:onboarding:source_type` em `Task.extra_data["work_queue_equivalence_key"]`; (3) para compatibilidade, aceitar task legada do mesmo `gym_id`, `member_id` e categoria/source de onboarding; (4) criar apenas se nenhuma candidata existir. A definicao ativa e concreta: `Task.status` em `TaskStatus.TODO` ou `TaskStatus.DOING`, `deleted_at is None` e `not is_task_operationally_archived(task)` usando `app.services.task_service.is_task_operationally_archived`/`operational_archive.archived_at`. Nao inventar colunas/status; strings legadas so podem ser normalizadas a partir de `extra_data` quando houver evidencia persistida. Ordenar candidatas deterministicamente por `created_at` e `id` para sempre escolher a mesma.

Ao reutilizar task legada, gravar `work_queue_equivalence_key`; ao criar, gravar a mesma chave desde o inicio. Atualizar `payload_snapshot.metadata.prepared_task_id` exclusivamente por `_persist_snapshot`, preservando o restante do snapshot, e devolver o UUID como `task_id` no schema de prepare e `canonical_task_id` no contrato da fila. Repeticoes sequenciais dentro da mesma sessao devem passar primeiro pelo link persistido. `AITriageRecommendation` nao possui nem deve ganhar coluna `prepared_task_id`. Nao adicionar constraint, migration, `SELECT FOR UPDATE`, retry concorrente ou claim; documentar no codigo apenas que a garantia concorrente e da Spec 055/Phase 10.1.
  </action>
  <acceptance_criteria>
    - A busca possui predicados explicitos de gym_id, member_id, onboarding/origem, `TaskStatus.TODO/DOING`, `deleted_at is None` e helper de arquivamento operacional.
    - `payload_snapshot.metadata.prepared_task_id` cross-tenant/inativo e rejeitado antes do fallback.
    - Task reutilizada ou criada recebe `work_queue_equivalence_key` e `_persist_snapshot` grava o task id no metadata antes do retorno.
    - Nenhuma coluna nova e presumida; o link permanece no snapshot e soft-delete/arquivamento usam os helpers reais.
    - Todos os casos de reuso/inatividade/repeticao em `test_ai_triage_service.py` passam.
    - O diff nao contem migration, unique constraint, lock, claim/version ou provider.
  </acceptance_criteria>
  <verify>
    <automated>cd saas-backend; py -3.12 -m pytest -q tests/test_ai_triage_service.py -k "prepare or reuse or task or tenant"</automated>
  </verify>
  <done>Preparacoes sequenciais convergem para uma task ativa canonica tenant-scoped, sem prometer seguranca concorrente ainda inexistente.</done>
</task>

<task id="10-02-03" type="auto" tdd="true">
  <name>Task 3: Expor task canonica, frescor e prontidao sem inventar dados</name>
  <read_first>
    saas-backend/app/schemas/work_queue.py
    saas-backend/app/services/work_queue_service.py
    saas-backend/app/schemas/ai_triage.py
    saas-backend/tests/test_work_queue_service.py
    saas-backend/tests/test_ai_triage_service.py
    saas-backend/app/models/task.py
    saas-backend/app/models/ai_triage_recommendation.py
    .planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-UI-SPEC.md
  </read_first>
  <files>saas-backend/app/schemas/work_queue.py, saas-backend/app/services/work_queue_service.py, saas-backend/tests/test_work_queue_service.py</files>
  <behavior>
    - WorkQueueItem inclui canonical_task_id e permite ao frontend distinguir continuar de criar.
    - Frescor usa `AITriageRecommendation.last_refreshed_at` ou `Task.updated_at`: ate 24h e fresh, acima de 24h e stale, unknown somente sem fonte temporal real.
    - readiness_missing_fields enumera assigned_to_name, assigned_to_role, due_at, decisive_reason, freshness ou signal ausentes sem bloquear por inferencia.
    - Somente ausencia comprovada preserva None/unknown; zero legado persistido permanece `signal_value=0`/`priority_state="known"` e nunca vira criticidade automatica.
    - Assignment e resolvido no carregamento em lote, tenant-scoped, sem consulta por item.
  </behavior>
  <action>
Estender de forma aditiva `WorkQueueItemOut` com `canonical_task_id: UUID | None`, `last_refreshed_at: datetime | None`, `freshness_state: Literal["fresh","stale","unknown"]`, `freshness_blocking: bool`, `readiness_missing_fields: list[str]`, `signal_value: float | None`, `priority_state: Literal["known","unknown"]`, `assigned_to_name: str | None` e `assigned_to_role: str | None`. Usar lista criada por factory/default_factory, nunca default mutavel.

Na projecao de AI triage, ler `payload_snapshot.metadata.prepared_task_id` como `canonical_task_id` e usar `AITriageRecommendation.last_refreshed_at` non-null como fonte temporal. Para items de Task, usar `Task.updated_at`; outras fontes sem timestamp confiavel retornam unknown. Definir uma constante unica `FRESHNESS_MAX_AGE = timedelta(hours=24)`: idade `<= 24h` e `fresh`, idade `> 24h` e `stale`, e `unknown` somente quando a fonte realmente nao fornece timestamp. Comparar datetimes timezone-aware com o relogio injetavel. `freshness_blocking` permanece `false` salvo se o backend ja possuir um motivo impeditivo explicito; stale/assignment/prazo ausente nao bloqueiam por inferencia.

Evitar N+1: `_list_task_items` deve aplicar `joinedload(Task.assigned_user)` na query ja presa a gym_id e `_task_to_item` deve usar somente essa relacao para `assigned_to_name`/`assigned_to_role`, sem lookup por item. Para AI triage, mapear `recommended_owner.label`/`recommended_owner.role` ja presentes no snapshot. Montar `readiness_missing_fields` com `assigned_to_name`, `assigned_to_role`, `due_at`, `decisive_reason`, `freshness`, `signal`.

Preservar provenance do sinal: chave/campo comprovadamente ausente produz `signal_value=None`, `priority_state="unknown"` e readiness `signal`; valor legado persistido igual a zero, mesmo sem provenance adicional, produz `signal_value=0`, `priority_state="known"`, nao adiciona `signal` as lacunas e nunca recebe severidade critical/danger por inferencia. Adicionar `test_wq_readiness_legacy_zero_is_known_noncritical` e um caso separado de ausencia real. Unknown ordena depois de valor conhecido; preservar due_at e motivo sem fabricar placeholders.
  </action>
  <acceptance_criteria>
    - O schema expõe os nove campos aditivos, incluindo assigned_to_name/assigned_to_role, com tipos opcionais/Literal e default_factory para a lista.
    - Uma recommendation com `payload_snapshot.metadata.prepared_task_id` serializa `canonical_task_id` igual ao UUID persistido.
    - Testes no limite exato de 24h e acima de 24h cobrem fresh/stale; unknown aparece somente sem timestamp real.
    - Cada lacuna canonica, inclusive assigned_to_name e assigned_to_role, passa nos testes focados.
    - `test_wq_readiness_legacy_zero_is_known_noncritical` prova zero/known/noncritical, enquanto o caso de chave ausente prova None/unknown.
    - Teste de lista prova joinedload de assigned_user, gym_id no statement e quantidade de queries constante ao aumentar items; nenhum lookup ocorre em `_task_to_item`.
    - Somente score comprovadamente ausente permanece None/unknown; zero persistido nunca e reclassificado como ausencia nem criticidade.
    - A suite combinada de AI triage e Work Queue fica verde sem regressao do Plan 10-01.
  </acceptance_criteria>
  <verify>
    <automated>cd saas-backend; py -3.12 -m pytest -q tests/test_ai_triage_service.py tests/test_work_queue_service.py</automated>
  </verify>
  <done>A API distingue task canonica, frescor e lacunas de prontidao com dados verificaveis e sem falsa criticidade.</done>
</task>

</tasks>

<verification>
Executar em `saas-backend`: `py -3.12 -m pytest -q tests/test_ai_triage_service.py tests/test_work_queue_service.py`. Confirmar duas preparacoes com mesmo UUID, `create_task.call_count == 1`, link no snapshot e `git diff --check` verde.
</verification>

<success_criteria>
- Recommendation ativa reutiliza task equivalente e repete o mesmo resultado sequencialmente.
- Task inativa/deletada/arquivada/cross-tenant nunca e reutilizada.
- Payload expõe task canonica, frescor e lacunas, preservando unknown.
- Nenhuma garantia estrutural da Phase 10.1 e implementada ou declarada concluida.
</success_criteria>

<output>
After completion, create `.planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-02-SUMMARY.md`
</output>
