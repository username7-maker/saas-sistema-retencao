---
phase: 10-integridade-operacional-da-fila-de-tasks-do-piloto
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - saas-backend/app/schemas/work_queue.py
  - saas-backend/app/routers/work_queue.py
  - saas-backend/app/services/work_queue_service.py
  - saas-backend/tests/test_work_queue_service.py
  - saas-backend/tests/test_work_queue_router.py
autonomous: true
requirements:
  - WQ-01
  - WQ-02
  - WQ-05
must_haves:
  truths:
    - "O operador alcanca a pagina 2 de um snapshot sintetico com 188 itens e encontra por busca um item que nao estava nos primeiros 25."
    - "A API informa total, pagina, tamanho, contagens por estado e fontes truncadas sem apresentar limite inferior como total exato."
    - "Um item adiado fica fora de do_now ate o visible_from canonico e empates priorizam o menor prazo antes da chave estavel."
    - "Busca, contagens e itens permanecem tenant-scoped e respeitam todos os filtros aplicaveis."
  artifacts:
    - path: "saas-backend/app/schemas/work_queue.py"
      provides: "Envelope tipado e aditivo da lista operacional"
      contains: "truncated_sources"
    - path: "saas-backend/app/services/work_queue_service.py"
      provides: "Busca, filtros, contagens, paginacao, visibilidade e ordenacao autoritativas"
      contains: "state_counts"
    - path: "saas-backend/tests/test_work_queue_service.py"
      provides: "Regressao de 188 itens, truncamento, snooze e desempate"
  key_links:
    - from: "saas-backend/app/routers/work_queue.py"
      to: "saas-backend/app/services/work_queue_service.py"
      via: "q/page/page_size e filtros tenant-scoped"
      pattern: "page_size"
    - from: "saas-backend/app/services/work_queue_service.py"
      to: "saas-backend/app/schemas/work_queue.py"
      via: "envelope items/total/state_counts/truncated_sources"
      pattern: "truncated_sources"
---

<objective>
Tornar o backend da Work Queue alcancavel e honesto antes de qualquer integracao de UI.

Purpose: Fechar WQ-01, WQ-02 e WQ-05 com busca/paginacao server-side, contagens coerentes, truncamento explicito, snooze canonico e ordenacao deterministica.
Output: Contrato de lista aditivo, agregacao corrigida e regressao backend focada.

Boundary: WQ-06, WQ-07, WQ-09, migrations, claim/CAS, consentimento, idempotencia de efeito e qualquer chamada de provider pertencem exclusivamente a Phase 10.1 e nao podem ser implementados neste plano.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-CONTEXT.md
@.planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-PRD.md
@.planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-RESEARCH.md
@.planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-VALIDATION.md
@specs/054-work-queue-integrity-p0/spec.md
@specs/054-work-queue-integrity-p0/plan.md
</context>

<tasks>

<task id="10-01-01" type="auto" tdd="true">
  <name>Task 1: Wave 0 - congelar os contratos de alcance e verdade</name>
  <read_first>
    saas-backend/tests/test_work_queue_service.py
    saas-backend/tests/conftest.py
    saas-backend/app/main.py
    saas-backend/tests/test_body_composition_kommo_router.py
    saas-backend/app/services/work_queue_service.py
    saas-backend/app/routers/work_queue.py
    saas-backend/app/schemas/work_queue.py
    .planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-VALIDATION.md
    specs/054-work-queue-integrity-p0/spec.md
  </read_first>
  <files>saas-backend/tests/test_work_queue_service.py, saas-backend/tests/test_work_queue_router.py</files>
  <behavior>
    - Dataset tenant-scoped com 188 itens: page=2/page_size=25 devolve itens 26-50 sem repetir a pagina 1.
    - Busca por aluno, motivo ou acao que existe apenas depois da posicao 25 devolve o item correto na pagina 1 da busca.
    - state_counts ignora somente o filtro de estado, respeita os demais filtros e usa elegibilidade efetiva; snoozed/cold-base/stale nao entram em do_now.
    - Cada origem que entrega cap+1 e marcada em truncated_sources; a sentinela extra nunca entra em items, total ou state_counts.
    - source=assessment_queue executa o loader dedicado; cada query/statement das cinco origens contem gym_id do tenant autenticado.
    - visible_from futuro exclui o item de do_now; no empate do valor de `_work_item_score`, due_at menor vence e source_type/str(source_id) estabilizam o resultado.
    - Um TestClient in-process com overrides sinteticos prova o envelope da rota sem credencial, banco ou provider real.
  </behavior>
  <action>
Adicionar primeiro testes `test_wq_*` RED, usando apenas fixtures sinteticas e relogio deterministico. Em `test_work_queue_service.py`, criar casos para dataset de 188 itens, pagina 2, busca depois dos 25 primeiros, `source=assessment_queue`, state_counts por elegibilidade efetiva, caps, predicado `gym_id` em cada loader/statement, snooze legado e desempate por `_work_item_score`. O teste tenant deve exercitar/inspecionar cada query de origem; misturar DTOs de tenants depois do loader nao prova isolamento. Criar `test_work_queue_router.py` seguindo o padrao de TestClient/dependency overrides de `test_body_composition_kommo_router.py`, para o GET real, as seis chaves do envelope e 422 para `page=0`, `page_size=0` e `page_size=101`. Incluir `test_wq_synthetic_smoke` com TestClient, auth/tenant overrides e service mock que devolve envelope sintetico; ele nao acessa credencial, banco ou provider. Rodar o slice `-k "wq_"`, registrar no `10-01-SUMMARY.md` IDs e motivos RED esperados, e so entao implementar; collect-only serve apenas como gate adicional de coleta.
  </action>
  <acceptance_criteria>
    - A execucao `-k "wq_"` fica RED antes da implementacao por assertions esperadas, nunca por fixture/import/coleta, e IDs/motivos ficam no summary.
    - `test_work_queue_service.py` cobre 188 itens, pagina 2, busca alem de 25, assessment_queue explicito, elegibilidade efetiva, gym_id por loader, truncamento, visible_from e due_at.
    - `test_work_queue_router.py` valida `items`, `total`, `page`, `page_size`, `state_counts`, `truncated_sources` e os tres 422.
    - `test_wq_synthetic_smoke` usa TestClient/overrides in-process e valida status 200 + envelope sem credencial externa.
    - Nenhum teste novo referencia tenant, token, telefone, email ou provider real.
  </acceptance_criteria>
  <verify>
    <automated>cd saas-backend; $out = py -3.12 -m pytest -q tests/test_work_queue_service.py tests/test_work_queue_router.py -k "wq_" 2&gt;&amp;1; $code = $LASTEXITCODE; $text = $out -join "`n"; $out; if ($code -eq 0 -or $text -notmatch "test_wq_") { exit 1 } else { exit 0 }</automated>
  </verify>
  <done>Os testes Wave 0 executam RED com IDs/motivos registrados e cobrem os seams reais de WQ-01/WQ-02/WQ-05 antes da implementacao.</done>
</task>

<task id="10-01-02" type="auto" tdd="true">
  <name>Task 2: Implementar envelope, busca, contagens e truncamento antes da pagina</name>
  <read_first>
    saas-backend/app/schemas/work_queue.py
    saas-backend/app/routers/work_queue.py
    saas-backend/app/services/work_queue_service.py
    saas-backend/tests/test_work_queue_service.py
    saas-backend/tests/test_work_queue_router.py
    .planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-RESEARCH.md
  </read_first>
  <files>saas-backend/app/schemas/work_queue.py, saas-backend/app/routers/work_queue.py, saas-backend/app/services/work_queue_service.py</files>
  <behavior>
    - O router aceita q opcional com trim, page >= 1 e page_size entre 1 e 100.
    - Filtros e busca sao aplicados antes do recorte global; total descreve o conjunto conhecido do estado selecionado.
    - state_counts e calculado da classificacao efetiva antes de aplicar apenas o estado selecionado, preservando os demais filtros.
    - source=assessment_queue aciona seu loader; busca dessa fonte e pos-cap e o cap atingido produz lower-bound explicito.
    - A resposta identifica cada fonte limitada e nunca afirma total exato quando truncated_sources nao esta vazio.
  </behavior>
  <action>
Criar em `schemas/work_queue.py` um envelope aditivo com `items: list[WorkQueueItemOut]`, `total: int`, `page: int`, `page_size: int`, `state_counts: dict[WorkQueueState, int]` e `truncated_sources: list[WorkQueueSourceType]`. Alterar `GET /api/v1/work-queue/items` para aceitar `q: str | None`, `page: int = 1` e `page_size: int = 25`, validar 1..100 via Query e repassar sempre o `gym_id` autenticado.

No service, fazer o branch de assessment rodar quando `source` for `all` ou `assessment_queue`. Preservar tetos (`task=300`, `ai_triage=200`, `ai_service_agent=100`, `student_personal_ai=100`, `assessment_queue=200`), consultar `cap + 1`, marcar truncamento tipado e remover a sentinela. Aplicar `q.strip().casefold()` nos campos persistidos antes do cap para as quatro fontes sob ownership. Como `assessment_queue` depende de `assessment_analytics_service` fora deste ownership, pesquisar essa origem depois do cap e, quando o cap for atingido, manter `assessment_queue` em `truncated_sources` para que resultado/total sejam declarados lower-bound; nao prometer busca completa dessa origem truncada. Garantir predicado `gym_id` em cada loader/statement.

Depois de materializar, aplicar filtros e a funcao existente de classificacao/elegibilidade efetiva antes das contagens: snoozed, cold-base e stale nao podem contar em `do_now`. Calcular state_counts ignorando somente o tab de estado, aplicar o estado, ordenar e somente entao paginar. `total` e o tamanho conhecido do estado selecionado e e lower-bound quando houver truncamento. Normalizar parametros legados internamente sem remover consumidor existente.
  </action>
  <acceptance_criteria>
    - O schema tipa `state_counts` com WorkQueueState e `truncated_sources` com WorkQueueSourceType.
    - O router rejeita page 0 e page_size 0/101 e mantem `gym_id` derivado da sessao, nunca do request.
    - source=assessment_queue chama o loader sem source=all; busca pos-cap/truncada dessa fonte e rotulada lower-bound.
    - Cada fonte consulta cap+1, remove a sentinela e adiciona seu nome apenas uma vez a `truncated_sources`.
    - Cada query/statement prova gym_id; state_counts efetivo exclui snoozed/cold-base/stale de do_now.
    - Nenhuma migration, claim/version, consent check, idempotency key ou provider call e adicionada.
  </acceptance_criteria>
  <verify>
    <automated>cd saas-backend; py -3.12 -m pytest -q tests/test_work_queue_service.py tests/test_work_queue_router.py -k "wq_"</automated>
  </verify>
  <done>O endpoint entrega um snapshot navegavel e pesquisavel com metadados honestos, mantendo caps tecnicos visiveis e isolamento tenant-scoped.</done>
</task>

<task id="10-01-03" type="auto" tdd="true">
  <name>Task 3: Canonizar snooze e tornar o desempate deterministico</name>
  <read_first>
    saas-backend/app/services/work_queue_service.py
    saas-backend/tests/test_work_queue_service.py
    saas-backend/app/schemas/work_queue.py
    .planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-CONTEXT.md
  </read_first>
  <files>saas-backend/app/services/work_queue_service.py, saas-backend/tests/test_work_queue_service.py</files>
  <behavior>
    - Escrita de snooze persiste `work_queue_visible_from` e mantem leitura de `work_queue_snoozed_until` legado.
    - Enquanto now for menor que visible_from, o item nao pertence a do_now; no instante exato ele volta a ser elegivel.
    - `score, _legacy_due = _work_item_score(item, now)` fornece o score escalar; em empate, due_at e source_type/str(source_id) estabilizam.
  </behavior>
  <action>
Centralizar a resolucao temporal em um helper unico: ler primeiro `extra_data["work_queue_visible_from"]`, usar `extra_data["work_queue_snoozed_until"]` apenas como fallback legado e normalizar timestamps timezone-aware. Na mutacao de snooze, persistir o instante canonico em `work_queue_visible_from`, manter `work_queue_snoozed_until` durante a compatibilidade e devolver o `visible_from` confirmado pelo backend; nao tratar todo `due_at` futuro como snooze. Classificar o item fora de `do_now` somente quando `now < visible_from`.

Para cada item, desembrulhar a assinatura real com `score, _legacy_due = _work_item_score(item, now)` e ordenar pela chave `(-score, item.due_at is None, due_at_or_max, item.source_type, str(item.source_id))`. `due_at_or_max` deve ser timezone-aware e usar `item.due_at`, nao `_legacy_due`, para manter prazo menor/null-last autoritativo. Um refactor para helper escalar `_work_item_priority_score(item, now)` e aceitavel somente se preservar a tupla existente para consumidores atuais. Nao negar a assinatura tuple, antecipar sinais do Plano 10-02 ou introduzir migration/claim.
  </action>
  <acceptance_criteria>
    - `work_queue_visible_from` e a primeira fonte de leitura e e escrito pela mutacao de snooze.
    - `work_queue_snoozed_until` continua aceito como fallback para dados legados.
    - Testes provam ausencia antes do instante, elegibilidade no instante e retorno canonico ao frontend.
    - Testes provam unpack de `_work_item_score(item, now)`, score escalar descendente, due_at ascendente/null-last e estabilidade por source_type/str(source_id).
    - O slice completo preserva o baseline backend e sai verde.
  </acceptance_criteria>
  <verify>
    <automated>cd saas-backend; py -3.12 -m pytest -q tests/test_work_queue_service.py tests/test_work_queue_router.py</automated>
  </verify>
  <done>Snooze e ordenacao refletem um unico estado temporal verificavel, com compatibilidade legada e resultado deterministico.</done>
</task>

</tasks>

<verification>
Executar em `saas-backend`: `py -3.12 -m pytest -q tests/test_work_queue_service.py tests/test_work_queue_router.py`. Confirmar tambem `git diff --check` na raiz e revisar que o diff nao contem migration, claim, consentimento, idempotencia de efeito ou provider.
</verification>

<success_criteria>
- Dataset sintetico de 188 itens permite pagina 2 e busca alem dos primeiros 25.
- Total, contagens e truncamento sao coerentes com filtros e nao mentem sobre caps.
- Snooze remove de do_now ate o instante canonico e prazo menor vence empates.
- Toda consulta continua presa ao gym_id autenticado.
</success_criteria>

<output>
After completion, create `.planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-01-SUMMARY.md`
</output>
