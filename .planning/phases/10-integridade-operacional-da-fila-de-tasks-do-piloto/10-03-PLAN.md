---
phase: 10-integridade-operacional-da-fila-de-tasks-do-piloto
plan: "03"
type: execute
wave: 3
depends_on:
  - "10-01"
  - "10-02"
files_modified:
  - saas-frontend/src/types/index.ts
  - saas-frontend/src/services/workQueueService.ts
  - saas-frontend/src/components/workQueue/WorkExecutionView.tsx
  - saas-frontend/src/components/ui2/Pagination.tsx
  - saas-frontend/src/test/WorkExecutionView.test.tsx
  - saas-frontend/src/test/AITriageInboxPage.test.tsx
  - saas-frontend/src/test/TasksPage.test.tsx
autonomous: true
requirements:
  - WQ-01
  - WQ-02
  - WQ-03
  - WQ-04
  - WQ-05
  - WQ-08
must_haves:
  truths:
    - "O operador pesquisa no servidor, navega para pagina 2 e ve faixa/total ou Pelo menos N sem depender de items.length."
    - "Contadores carregam com reticencias/estado busy e nunca exibem zero provisorio."
    - "Recommendation vinculada oferece Continuar tarefa; stale, unknown, assigned_to_name/assigned_to_role e prazo ausentes usam copy textual honesta."
    - "Snooze confirmado remove o card de Fazer agora, seleciona o proximo e anuncia o horario canonico de retorno."
    - "/tasks e /ai/triage exercitam o mesmo WorkExecutionView e passam pela mesma regressao compartilhada."
  artifacts:
    - path: "saas-frontend/src/services/workQueueService.ts"
      provides: "Cliente tipado do envelope e parametros remotos"
      contains: "truncated_sources"
    - path: "saas-frontend/src/components/workQueue/WorkExecutionView.tsx"
      provides: "Runner compartilhado com busca, navegacao, estados honestos e selecao estavel"
      contains: "Continuar tarefa"
    - path: "saas-frontend/src/test/WorkExecutionView.test.tsx"
      provides: "Regressao comum consumida pelas duas superficies"
      min_lines: 1
    - path: "saas-backend/tests/test_work_queue_router.py"
      provides: "Smoke sintetico in-process do envelope HTTP"
      contains: "synthetic_smoke"
    - path: ".planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-03-SUMMARY.md"
      provides: "Comandos e saidas dos smokes sinteticos e do full gate"
      contains: "smoke sintetico"
  key_links:
    - from: "saas-frontend/src/components/workQueue/WorkExecutionView.tsx"
      to: "saas-frontend/src/services/workQueueService.ts"
      via: "q/page/page_size/filtros e envelope autoritativo"
      pattern: "page_size"
    - from: "saas-frontend/src/test/TasksPage.test.tsx"
      to: "saas-frontend/src/components/workQueue/WorkExecutionView.tsx"
      via: "contrato compartilhado da rota"
      pattern: "WorkExecutionView"
    - from: "saas-frontend/src/test/AITriageInboxPage.test.tsx"
      to: "saas-frontend/src/components/workQueue/WorkExecutionView.tsx"
      via: "contrato compartilhado da rota"
      pattern: "WorkExecutionView"
---

<objective>
Consumir o snapshot autoritativo no runner compartilhado e fechar a regressao completa do piloto local.

Purpose: Entregar a experiencia staff-first de WQ-01/02/03/04/05 e provar WQ-08 nas duas rotas sem redesenho lateral.
Output: Tipos/cliente aditivos, WorkExecutionView navegavel e suite frontend/backend/build/lint/spec verde.

Boundary: Nao criar UI, copy ou chamadas para claim/version, conflitos 409, consentimento, idempotencia ou provider. WQ-06, WQ-07 e WQ-09 pertencem integralmente a Phase 10.1. Nao tocar `src/components/tasks/*` nem a lista completa legada.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-CONTEXT.md
@.planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-PRD.md
@.planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-RESEARCH.md
@.planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-VALIDATION.md
@.planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-UI-SPEC.md
@specs/054-work-queue-integrity-p0/spec.md
@.planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-01-SUMMARY.md
@.planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-02-SUMMARY.md
</context>

<tasks>

<task id="10-03-01" type="auto" tdd="true">
  <name>Task 1: Wave 0 - criar a regressao do runner e reparar o baseline de Tasks</name>
  <read_first>
    saas-frontend/src/test/TasksPage.test.tsx
    saas-frontend/src/test/AITriageInboxPage.test.tsx
    saas-frontend/src/components/workQueue/WorkExecutionView.tsx
    saas-frontend/src/services/workQueueService.ts
    saas-frontend/src/types/index.ts
    .planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-VALIDATION.md
    .planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-UI-SPEC.md
  </read_first>
  <files>saas-frontend/src/test/WorkExecutionView.test.tsx, saas-frontend/src/test/TasksPage.test.tsx, saas-frontend/src/test/AITriageInboxPage.test.tsx</files>
  <behavior>
    - Debounce de 300ms envia q/filtros/page=1; Enter antecipa a busca e limpar busca volta para pagina 1.
    - Pagina 2 envia page=2/page_size=25, preserva conteudo durante loading e seleciona o primeiro card apos sucesso por teclado.
    - Uma unica query ativa usa o envelope/state_counts; loading mostra reticencias, erro mostra traco e truncamento mostra N+/Pelo menos N.
    - canonical_task_id produz Continuar tarefa, carrega o item task read-only e nunca dispara prepare/create/execute.
    - As duas paginas continuam montando o mesmo WorkExecutionView.
    - Cada wrapper possui um smoke sintetico in-process com MemoryRouter, QueryClient e workQueueService mockado.
  </behavior>
  <action>
Criar `WorkExecutionView.test.tsx` antes da implementacao com titulos prefixados `WQ runner`, Vitest fake timers, Testing Library e mock tipado do envelope. Cobrir busca/debounce/Enter/clear/retry, pagina 2, query unica/state_counts, selecao, fresh/stale/unknown, assignment, lacunas e snooze. Para task canonica, provar `getItem('task', canonical_task_id)`, override `task:id`, zero prepare/create/execute, limpeza e 404 recuperavel. Usar apenas fixtures sinteticas.

Reparar os tres failures preexistentes de `TasksPage.test.tsx`: atualizar a copy acentuada e substituir os dois seletores ambiguos de `Onboarding` por role/nome com `within`, sem seletor posicional. Em ambos os wrappers, afirmar filtro inicial/wiring e adicionar casos intitulados `smoke sintetico /tasks` e `smoke sintetico /ai/triage`, montando a rota com `MemoryRouter`, `QueryClient` isolado e `workQueueService` mockado para devolver envelope sintetico. Os wrappers devem ficar verdes; a nova suite do runner deve executar RED somente nos titulos `WQ runner`, nunca por import/fixture/coleta, e os IDs/motivos devem ir ao `10-03-SUMMARY.md`.
  </action>
  <acceptance_criteria>
    - `WorkExecutionView.test.tsx` cobre debounce 300ms, page 2, query unica, reticencias, N+/Pelo menos N, getItem canonico sem mutacao, 404, stale/unknown e snooze.
    - Todos os testes de `TasksPage.test.tsx` passam, incluindo seu smoke sintetico, e nenhum seletor posicional resolve Onboarding.
    - Todos os testes de `AITriageInboxPage.test.tsx` passam, incluindo seu smoke sintetico, e o wrapper comprova uso do WorkExecutionView.
    - Os casos `smoke sintetico` das duas rotas usam MemoryRouter/QueryClient/service mock e nao acessam rede/credencial.
    - O RED guard executa `WorkExecutionView.test.tsx`, encontra titulos `WQ runner` esperados e rejeita erro de import/fixture/coleta.
    - Todos os dados dos testes sao sinteticos e nenhum teste chama API/provider real.
  </acceptance_criteria>
  <verify>
    <automated>cd saas-frontend; npm.cmd test -- --run src/test/TasksPage.test.tsx src/test/AITriageInboxPage.test.tsx; if ($LASTEXITCODE -ne 0) { exit 1 }; $red = (npm.cmd test -- --run src/test/WorkExecutionView.test.tsx | Out-String); $code = $LASTEXITCODE; Write-Output $red; if ($code -eq 0 -or $red -notmatch "WorkExecutionView.test.tsx" -or $red -notmatch "WQ runner") { exit 1 } else { exit 0 }</automated>
  </verify>
  <done>O baseline das rotas fica verde e a suite RED do runner descreve integralmente o contrato compartilhado antes da implementacao.</done>
</task>

<task id="10-03-02" type="auto" tdd="true">
  <name>Task 2: Integrar envelope remoto e estados honestos no WorkExecutionView</name>
  <read_first>
    saas-frontend/src/types/index.ts
    saas-frontend/src/services/workQueueService.ts
    saas-frontend/src/components/workQueue/WorkExecutionView.tsx
    saas-frontend/src/components/ui2/Pagination.tsx
    saas-frontend/src/test/WorkExecutionView.test.tsx
    .planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-UI-SPEC.md
    .planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-02-SUMMARY.md
  </read_first>
  <files>saas-frontend/src/types/index.ts, saas-frontend/src/services/workQueueService.ts, saas-frontend/src/components/workQueue/WorkExecutionView.tsx, saas-frontend/src/components/ui2/Pagination.tsx</files>
  <behavior>
    - O frontend envia filtros/q/page/page_size e renderiza metadados do envelope, nunca items.length como total.
    - Existe uma unica useQuery ativa com key `[work-queue,mode,source,domain,bucket,shift,assignee,page,q]`; counts vem do state_counts desse envelope.
    - Primeira carga usa skeleton; busca/pagina/refresh mantem o ultimo sucesso visivel com aria-busy.
    - Selecao usa source_type:source_id, sobrevive a refresh quando elegivel e avanca apos snooze/outcome.
    - Continuar tarefa usa GET read-only e inspector override; UI comunica frescor/ausencias e so bloqueia quando freshness_blocking e explicito.
  </behavior>
  <action>
Adicionar aos tipos o envelope e os campos backend dos Planos 01/02 sem `any`. Atualizar `workQueueService` para serializar `q`, `page`, `page_size=25`, domain, state, shift, assignee, source e bucket; retornar o envelope inteiro, preservar erros para retry e reutilizar o GET read-only `getItem(source_type, source_id)` existente.

No `WorkExecutionView`, substituir as tres queries lazy `do_now`/`awaiting`/`all` por exatamente uma `useQuery` ativa com key `["work-queue", mode, source, domain, bucket, shift, assignee, page, q]`. O estado selecionado filtra a apresentacao/parametro sem criar outra consulta e ambos os counters usam `state_counts` do mesmo envelope; nunca manter query de tab inativo exibindo zero. Manter estado separado para input e q aplicado, debounce 300ms, Enter imediato e page 1 em busca/filtro. Preservar o ultimo envelope durante refetch, marcar `aria-busy` e usar `source_type:source_id` como chave. Renderizar counts com reticencias/ultimo valor+Atualizando/traco/N+, faixa exata ou `pelo menos N`, badge/explicacao de truncamento e Pagination acessivel com controles direcionais no mobile.

Quando `canonical_task_id` existir, `Continuar tarefa` chama `workQueueService.getItem('task', canonical_task_id)`, guarda o retorno como inspector override/selection `task:{id}` e nao chama prepare/create/execute. Selecionar outro card limpa o override. Em 404, manter a recommendation e mostrar `Tarefa vinculada indisponível.` + `Atualizar fila`; nao navegar para query param/rota inexistente. Renderizar assignment/frescor/lacunas com a copy aprovada e bloquear apenas com `freshness_blocking=true`. Depois de snooze confirmado, usar `visible_from`, remover de Fazer agora, selecionar/focar proximo-anterior-empty e anunciar o retorno. Reusar apenas componentes/tokens Cordex; nao tocar lista legada nem estados da Phase 10.1.
  </action>
  <acceptance_criteria>
    - A chamada de lista inclui q/page/page_size e todos os filtros; nenhuma faixa/contagem usa items.length como total.
    - O componente possui uma unica useQuery ativa com a key exata e ambos os counters derivam do state_counts retornado.
    - Debounce e 300ms, Enter e imediato, nova busca volta a page 1 e erro preserva o ultimo resultado.
    - Loading, count error, total exato e truncado usam exatamente a copy/semantica aprovada e nomes acessiveis.
    - canonical_task_id chama getItem task, cria override local e deixa prepare/create/execute com call count zero; 404 mostra a recuperacao aprovada.
    - Snooze usa o visible_from da resposta e atualiza lista, selecao, foco e live region.
    - `npm.cmd test -- --run src/test/WorkExecutionView.test.tsx` sai verde.
  </acceptance_criteria>
  <verify>
    <automated>cd saas-frontend; npm.cmd test -- --run src/test/WorkExecutionView.test.tsx</automated>
  </verify>
  <done>O runner compartilha um snapshot remoto navegavel, mantem contexto durante revalidacao e comunica todos os limites/lacunas sem inferencia enganosa.</done>
</task>

<task id="10-03-03" type="auto">
  <name>Task 3: Fechar smoke sintetico cruzado e preparar o full gate</name>
  <read_first>
    saas-frontend/src/test/WorkExecutionView.test.tsx
    saas-frontend/src/test/TasksPage.test.tsx
    saas-frontend/src/test/AITriageInboxPage.test.tsx
    saas-frontend/src/components/workQueue/WorkExecutionView.tsx
    .planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-VALIDATION.md
    specs/054-work-queue-integrity-p0/tasks.md
  </read_first>
  <files>saas-frontend/src/test/WorkExecutionView.test.tsx, saas-frontend/src/test/TasksPage.test.tsx, saas-frontend/src/test/AITriageInboxPage.test.tsx</files>
  <action>
Completar as assertions de integracao das duas rotas usando a mesma fixture/envelope e o mesmo `WorkExecutionView`; cada wrapper testa titulo/filtro inicial/wiring e seu smoke sintetico, enquanto busca, pagina, contagens, task canonica, stale/unknown e snooze permanecem na suite compartilhada. Confirmar que o full gate final inclui exatamente cinco arquivos backend (`test_work_queue_service.py`, `test_work_queue_router.py`, `test_ai_triage_service.py`, `test_ai_triage_router.py`, `test_autopilot_services.py`), mas nao executa-lo como feedback rapido desta task.

Rodar apenas o smoke rapido: backend `test_work_queue_router.py -k synthetic_smoke` via TestClient/overrides e frontend os dois casos `smoke sintetico` via MemoryRouter/QueryClient/service mock. Registrar comandos, exit codes e resumo das saidas em `10-03-SUMMARY.md`. Nao acessar rede, credencial ou publicacao e nao declarar a borda publicada validada. O full gate agregado, incluindo baseline de dois warnings preexistentes em MethodOsPage e zero novos, fica exclusivamente no bloco `<verification>` final.
  </action>
  <acceptance_criteria>
    - `test_work_queue_router.py -k synthetic_smoke` passa in-process com envelope sintetico e zero rede/credencial.
    - Os casos frontend `smoke sintetico /tasks` e `smoke sintetico /ai/triage` passam com MemoryRouter, QueryClient e service mock.
    - `10-03-SUMMARY.md` registra os tres comandos, exit code 0 e saidas resumidas separadas por backend/frontend.
    - O quick check nao executa nem substitui o full gate final e nenhum requisito da Phase 10.1 e declarado concluido.
  </acceptance_criteria>
  <verify>
    <automated>cd saas-backend; py -3.12 -m pytest -q tests/test_work_queue_router.py -k synthetic_smoke; if ($LASTEXITCODE -ne 0) { exit 1 }; cd ../saas-frontend; npm.cmd test -- --run src/test/AITriageInboxPage.test.tsx src/test/TasksPage.test.tsx -t "smoke sintetico"</automated>
  </verify>
  <done>Os smokes sinteticos backend e das duas superficies passam e suas saidas ficam registradas, deixando o full gate agregado para a verificacao final.</done>
</task>

</tasks>

<verification>
Somente depois das tres tasks, executar o full gate agregado. Em `saas-backend`: `py -3.12 -m pytest -q tests/test_work_queue_service.py tests/test_work_queue_router.py tests/test_ai_triage_service.py tests/test_ai_triage_router.py tests/test_autopilot_services.py`. Em `saas-frontend`: `npm.cmd test -- --run src/test/WorkExecutionView.test.tsx src/test/AITriageInboxPage.test.tsx src/test/TasksPage.test.tsx`, `npm.cmd run build`, `npm.cmd run lint`; lint aceita exatamente os dois warnings preexistentes em MethodOsPage, zero errors e zero warning novo. Na raiz: `specify check` e `git diff --check`. O full gate nao herda o teto de 30 segundos reservado aos checks por task.
</verification>

<success_criteria>
- /tasks e /ai/triage compartilham busca, pagina, contagens, selecao, task canonica, frescor e snooze.
- O runner nunca mostra zero provisorio nem limite inferior como total exato.
- Frontend/backend focados, build, lint, Spec Kit e diff passam no baseline declarado.
- O diff nao toca a lista legada nem implementa WQ-06/WQ-07/WQ-09.
</success_criteria>

<output>
After completion, create `.planning/phases/10-integridade-operacional-da-fila-de-tasks-do-piloto/10-03-SUMMARY.md`
</output>
