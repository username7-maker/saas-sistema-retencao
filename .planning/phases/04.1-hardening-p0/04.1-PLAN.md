# Phase 4.1 Plan

## Workstreams

1. Corrigir parsing e fallback de aniversario importado em automacoes e dashboards.
2. Substituir thread daemon de recalc por fila duravel com status consultavel.
3. Migrar broadcast local de WebSocket para Redis Pub/Sub mantendo o contrato do cliente.
4. Restaurar suites backend/frontend para estado totalmente verde.

## Acceptance

- `birthday_label` entra em aniversarios e automacoes.
- `POST /members/recalculate-risk` retorna `202` com `request_id`.
- existe `GET /members/recalculate-risk/{request_id}`.
- realtime entre workers deixa de depender do processo local.
- `pytest`, `npm run test`, `npm run lint` e `npm run build` passam.
