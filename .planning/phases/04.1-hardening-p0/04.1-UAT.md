# Phase 4.1 UAT

## Verified locally

- membro sem `birthdate`, mas com `birthday_label`, aparece corretamente em aniversarios
- automacao de aniversario aceita o fallback importado
- recalc manual de risco gera solicitacao com `request_id`
- status da solicitacao pode ser consultado
- websocket manager usa Redis Pub/Sub sem mudar contrato do cliente
- suites backend/frontend, lint e build ficaram verdes
