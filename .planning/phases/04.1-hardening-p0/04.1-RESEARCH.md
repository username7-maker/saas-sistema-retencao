# Phase 4.1 Research

- O backend ja tinha worker, scheduler e Redis; o melhor hardening era reaproveitar essa base em vez de criar outro mecanismo de fila.
- O contrato do cliente WebSocket ja estava embutido em varias telas; mudar apenas a infraestrutura do servidor reduzia risco.
- O fallback de aniversario precisava continuar aceitando base importada parcial, porque a academia real nao nasce com `birthdate` completo em todos os membros.
