# Phase 4.31 Status

## Status

`published no piloto` e `validado com smoke tecnico`

## O que ficou pronto

- contrato backend de sync do Actuar agora expõe `unsupported_fields`
- a UI de bioimpedancia diferencia com mais clareza:
  - fluxo manual
  - OCR local
  - leitura assistida por IA
  - OCR local com revisao manual
  - Actuar fora do escopo do ambiente
  - sync automatico em andamento
  - fluxo manual assistido
- a tela deixou de mostrar `sync_mode` cru e passou a usar labels humanas
- campos que permanecem apenas no AI GYM OS agora aparecem de forma explicita
- o CTA de copia fica mais honesto quando o ambiente esta em modo manual/desligado
- pills de sync ficaram coerentes com o risco do estado real

## Arquivos principais

- `saas-backend/app/schemas/body_composition.py`
- `saas-backend/app/services/body_composition_actuar_sync_service.py`
- `saas-backend/tests/test_body_composition_sync.py`
- `saas-backend/tests/test_body_composition_sync_router.py`
- `saas-frontend/src/components/assessments/MemberBodyCompositionTab.tsx`
- `saas-frontend/src/components/assessments/bodyCompositionCapability.ts`
- `saas-frontend/src/test/bodyCompositionCapability.test.ts`
- `saas-frontend/src/types/index.ts`

## Validacao executada

- `pytest saas-backend/tests/test_body_composition_sync.py saas-backend/tests/test_body_composition_sync_router.py`
- `npm run test -- src/test/bodyCompositionAssistedRead.test.ts src/test/bodyCompositionCapability.test.ts`
- `npm run lint`
- `npm run build`

## Resultado da validacao

- backend: `14 passed`
- frontend unit: `7 passed`
- frontend lint: `passed`
- frontend build: `passed`

## Deploy do piloto

- backend publicado no Railway com mensagem `phase 4.31 bioimpedance actuar readiness`
- worker publicado no Railway com a mesma mensagem
- frontend publicado na Vercel e promovido para o alias:
  - `https://saas-frontend-pearl.vercel.app`
- smoke tecnico publicado:
  - `GET /health/ready` = `200`
  - `GET /login` no frontend = `200`
  - login do owner no backend = `200`
- `GET /users/me` com token do owner = `200`
- login E2E no frontend publicado = `ok`
  - fluxo validado com Playwright ate `https://saas-frontend-pearl.vercel.app/dashboard/executive`
  - requests principais de `auth/login` e `users/me` responderam `200`

## Observacoes

- o piloto agora comunica melhor quando a IA assistida esta desligada ou quando o Actuar esta fora do escopo
- ainda existe um detalhe visual de encoding no texto de vinculo do Actuar dentro do componente, sem impacto funcional
- durante o debug do piloto publicado, a causa raiz do login quebrado foi confirmada:
  - o frontend havia sido buildado sem `VITE_API_BASE_URL`, caindo para `http://127.0.0.1:8000`
  - `VITE_API_BASE_URL` e `VITE_WS_BASE_URL` foram configuradas na Vercel de producao e o frontend foi redeployado
- ainda existe um bug separado de `mixed-content` em notificacoes, porque a tela publicada faz requests em `http://ai-gym-os-api-production.up.railway.app/...`

## Proximo passo recomendado

- validar no piloto publicado com `owner` e `trainer`:
  - leitura local sem IA
  - tentativa de leitura assistida com IA desligada
  - sync Actuar desabilitado
  - resumo para lancamento manual
