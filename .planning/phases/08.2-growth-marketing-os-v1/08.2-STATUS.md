# Phase 8.2 Status - Growth / Marketing OS V1

## Estado

- Status: `execute`
- Data: 2026-04-26
- Corte atual: T203 da spec `005-ai-first-os-academia-expansion`.

## Entregue neste corte

- Schemas de Growth:
  - `GrowthAudienceOut`;
  - `GrowthOpportunityOut`;
  - `GrowthOpportunityPrepareInput`;
  - `GrowthOpportunityPreparedOut`.
- Service `growth_service` com audiencias deterministicas para:
  - leads quentes;
  - leads parados;
  - alunos inativos;
  - renovacao com atencao;
  - promotores/NPS alto;
  - recuperacao de NPS baixo.
- Endpoints:
  - `GET /api/v1/crm/growth/audiences`;
  - `POST /api/v1/crm/growth/opportunities/{opportunity_id}/prepare`.
- Prepare operacional com:
  - URL de WhatsApp gerada no clique humano;
  - criacao de task opcional/interna;
  - nota CRM append-only para lead;
  - warning quando falta consentimento de comunicacao.
- CRM frontend ganhou painel `Growth OS` com audiencias, top oportunidades, canal, turno, score, consentimento e CTA de preparo.

## Decisoes

- Growth OS fica dentro do CRM nesta V1 para evitar novo modulo solto.
- Nao ha envio em massa nem campanha autonoma nesta fase.
- Consentimento de comunicacao nao bloqueia a geracao da oportunidade, mas aparece como warning antes da execucao humana.
- Kommo permanece como integracao segura existente; a acao de Growth V1 nao cria handoff Kommo automatico.

## Validacao executada

- `python -m pytest tests\test_growth_service.py tests\test_acquisition_service.py tests\test_compliance_service.py -q`: 9 passed.
- `python -c "from app.routers import crm; from app.services.growth_service import list_growth_audiences; print('t203-backend-ok')"`: OK.
- `npm.cmd run build` em `saas-frontend`: build TypeScript/Vite concluido com sucesso.

## Riscos e pendencias

- Ainda nao publicado no piloto.
- O painel depende de deploy backend + frontend juntos; frontend novo sem backend novo dara 404 em `/crm/growth/audiences`.
- Audiencias sao computadas sob demanda, sem historico de campanha/experimento persistido.
- O proximo corte pode persistir campaigns/outcomes se a equipe validar que o painel gera acao real.
