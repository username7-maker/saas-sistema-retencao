# 09.14 Plan - Resend Email Provider

1. Mapear pontos que usam `send_email_result` e `send_email_with_attachment_result`.
2. Trocar implementacao por Resend HTTP API com `httpx`.
3. Manter `EmailSendResult` como contrato interno.
4. Atualizar settings, env examples e validacao de producao.
5. Atualizar testes de auth, usuarios, config e dispatches.
6. Validar com pytests focados, compileall, `specify check` e busca por segredos.
7. Preparar instrucoes para Railway: usar chave nova, `RESEND_SENDER` de teste ou dominio verificado.
