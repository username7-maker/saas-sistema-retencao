# 09.8 - Validacao

## Comandos previstos
- `specify check`
- `cd saas-backend && pytest tests/test_auth_service_full.py tests/test_email_utils.py tests/test_user_admin_routes.py`
- `cd saas-frontend && npm.cmd run test -- src/test/ResetPasswordPage.test.tsx src/test/UsersPage.test.tsx src/test/LoginPage.test.tsx src/test/LovableLayout.test.tsx`
- `cd saas-frontend && npm.cmd run lint`
- `cd saas-frontend && npm.cmd run build`
- Busca por `temporary_password`, `password`, `token` em logs/auditoria alterados.

## Resultado
- `specify check`: passou antes e depois da implementacao.
- `cd saas-backend && python -m pytest tests/test_auth_service_full.py tests/test_email_utils.py tests/test_user_admin_routes.py tests/test_auth_router_session_cookies.py`: 59 passed.
- `cd saas-frontend && npm.cmd test -- --run src/test/LoginPage.test.tsx src/test/ResetPasswordPage.test.tsx src/test/UsersPage.test.tsx src/test/DashboardLovable.test.tsx`: 12 passed.
- `cd saas-frontend && npm.cmd run lint`: passou.
- `cd saas-frontend && npm.cmd run build`: passou.
- Busca por segredos: senha provisoria aparece apenas em response/UI imediato e testes; auditoria registra apenas `target_role`.
- Deploy backend Railway `ai-gym-os-api` em producao/piloto: deployment `1697bc1a-5be4-4e5f-9190-529d759eba60`, status `SUCCESS`.
- Health check backend: `https://ai-gym-os-api-production.up.railway.app/health` retornou HTTP 200 `{"status":"ok"}`.
- Deploy frontend Vercel `automai/saas-frontend`: deployment `dpl_BopBAcCBmyhMDpLTcZHBkoecnkUf`, alias `https://saas-frontend-pearl.vercel.app`.
- Smoke test publico: `/login` retornou HTTP 200 e Playwright confirmou `Esqueci minha senha` e `Enviar link de redefinicao` visiveis.

## Observacoes
- O teste planejado `LovableLayout.test.tsx` nao existe no projeto; a validacao visual conservadora ficou coberta por lint/build e pelo teste de dashboard existente.
- `npm` via PowerShell foi bloqueado por execution policy; os comandos foram executados com `npm.cmd`.

## Hotfix 2026-05-26 - SendGrid bloqueando recuperacao por e-mail
- Evidencia em logs de producao: backend recebeu a solicitacao, localizou tenant/usuario, mas o provedor recusou o envio. O app agora registra a razao de bloqueio do SendGrid no log sem expor segredo.
- Adicionado fallback operacional autenticado em `POST /api/v1/users/me/password`, exposto em Configuracoes > Seguranca como "Alterar minha senha".
- O endpoint valida a senha atual, grava o novo hash, limpa refresh/reset tokens e registra auditoria com `method=authenticated_change`, sem senha em logs.
- `cd saas-backend && python -m pytest tests/test_user_admin_routes.py tests/test_auth_service_full.py tests/test_email_utils.py tests/test_auth_router_session_cookies.py`: 61 passed.
- `cd saas-frontend && npm.cmd test -- --run src/test/SettingsPage.test.tsx src/test/LoginPage.test.tsx src/test/ResetPasswordPage.test.tsx src/test/UsersPage.test.tsx`: 10 passed.
- `specify check`, `npm.cmd run lint` e `npm.cmd run build`: passaram apos o hotfix.
- Deploy backend Railway `ai-gym-os-api`: deployment `859216ce-00af-49e8-a244-707ab845c661`, status `Online`; `/health` retornou HTTP 200.
- Deploy frontend Vercel: deployment `dpl_GoexNbNP9rQALQTCwXa73FnzF8Bk`, alias `https://saas-frontend-pearl.vercel.app`.
- Smoke do piloto: asset `SettingsPage-B9WvucTK.js` contem `Alterar minha senha` e `Recuperacao por e-mail`; `POST /api/v1/users/me/password` sem autenticacao retorna 401, confirmando rota protegida publicada.

## Hotfix 2026-05-26 - Criacao de usuario com senha definida pelo usuario
- Decisao de produto: criacao administrativa nao deve gerar senha provisoria por padrao. O padrao passa a ser convite por e-mail para o usuario definir a propria senha.
- `POST /api/v1/users/` aceita `password_setup=invite|temporary|manual`; sem parametro usa `invite`. `temporary` so gera e retorna senha provisoria quando solicitado explicitamente.
- Se o convite por e-mail falhar, a criacao faz rollback e orienta usar senha provisoria apenas se o usuario solicitou.
- `UsersPage` mostra "Usuario cria a senha por convite" como padrao e "Gerar senha provisoria agora" como escolha explicita.
- `cd saas-backend && python -m pytest tests/test_user_admin_routes.py tests/test_auth_service_full.py tests/test_email_utils.py tests/test_auth_router_session_cookies.py`: 66 passed.
- `cd saas-frontend && npm.cmd test -- --run src/test/UsersPage.test.tsx src/test/SettingsPage.test.tsx src/test/LoginPage.test.tsx src/test/ResetPasswordPage.test.tsx`: 11 passed.
- `specify check`, `npm.cmd run lint`, `npm.cmd run build`, `git diff --check` e busca por segredos nos arquivos tocados: passaram.
- Deploy backend Railway `ai-gym-os-api`: deployment `4495a1f9-91ef-4ecf-a984-c2eef9ececd3`, status `Online`; `/health` retornou HTTP 200.
- Deploy frontend Vercel: deployment `dpl_58EB5NyqtsPiZkdmzLMkZg5Fwnpy`, alias `https://saas-frontend-pearl.vercel.app`.
- Smoke do piloto: asset `UsersPage-CoRPqXOi.js` contem `Usuario cria a senha por convite` e `Gerar senha provisoria agora`; `POST /api/v1/users/` sem autenticacao retorna 401.

## Hotfix 2026-05-27 - Senha manual na criacao e diagnostico SendGrid
- Decisao de produto atualizada: criacao de usuario deve voltar a permitir digitar senha na criacao como fluxo principal.
- `UsersPage` exibe `Digitar senha agora` por padrao com confirmacao de senha, mantendo `Usuario cria a senha por convite` e `Gerar senha provisoria agora` como opcoes.
- `POST /api/v1/users/` usa `password_setup=manual` por padrao; `invite` e `temporary` continuam disponiveis explicitamente.
- Diagnostico GSD registrado em `.planning/debug/sendgrid-reset-email-credits-exceeded.md`.
- Teste controlado em producao indicou `Maximum credits exceeded` no SendGrid; codigo passou a classificar como `sendgrid_credits_exceeded` e a resposta do reset informa a causa operacional.
- `cd saas-backend && python -m pytest tests/test_user_admin_routes.py tests/test_auth_service_full.py tests/test_email_utils.py tests/test_auth_router_session_cookies.py`: 68 passed.
- `cd saas-frontend && npm.cmd test -- --run src/test/UsersPage.test.tsx src/test/SettingsPage.test.tsx src/test/LoginPage.test.tsx src/test/ResetPasswordPage.test.tsx`: 12 passed.
- `specify check`, `npm.cmd run lint`, `npm.cmd run build`, `git diff --check` e busca por segredos nos arquivos tocados: passaram.
- Deploy backend Railway `ai-gym-os-api`: deployment `cceb5114-87db-46fe-a66b-55529288b5c6`, status `Online`; `/health` retornou HTTP 200.
- Deploy frontend Vercel: deployment `dpl_ApJKS56pBcAo2XK1f2fCBPJYHZbe`, alias `https://saas-frontend-pearl.vercel.app`.
- Primeiro deploy Vercel falhou por erro transitorio da API (`Internal Server` no upload); retry passou.
- Smoke do piloto: asset `UsersPage-B_hTh_Nu.js` contem `Digitar senha agora`, `Minimo de 8 caracteres` e `Gerar senha provisoria agora`.
- Smoke do reset: `POST /api/v1/auth/forgot-password` para o usuario piloto retornou HTTP 503, consistente com SendGrid `Maximum credits exceeded`.

## Hotfix 2026-05-27 - Tasks Onboarding scoreboard
- Sintoma no piloto: `/tasks` > `Onboarding` mostrava `Nao foi possivel carregar os dados de onboarding.`
- Diagnostico: `GET /api/v1/onboarding/cockpit` respondia OK; `GET /api/v1/members/onboarding-scoreboard` recalculava score de todos os alunos em onboarding a cada abertura da aba.
- Evidencia em producao/piloto: 91 alunos candidatos; cada `calculate_onboarding_score` levou cerca de 1.88s, levando a requisicao para perto de 170s.
- Correcao: scoreboard passou a consultar snapshot persistido (`Member.onboarding_score` e `Member.onboarding_status`) por query direta; o recalculo detalhado permanece apenas no endpoint de detalhe do aluno.
- Diagnostico apos correcao com banco do piloto: 91 snapshots em 0.255s.
- `cd saas-backend && python -m pytest tests/test_members_onboarding_scoreboard_router.py`: 1 passed.
- `cd saas-backend && python -m pytest tests/test_members_onboarding_scoreboard_router.py tests/test_onboarding_score.py`: 11 passed.
- `specify check`: passou.
- Primeiro deploy Railway enviado da raiz do monorepo falhou por nao usar `saas-backend` como raiz; a API antiga permaneceu online.
- Deploy Railway correto via `railway up ./saas-backend --path-as-root`: `ca848b6a-6467-47a3-909f-1e34cae8b410`, status `SUCCESS`.
- Health check backend: `https://ai-gym-os-api-production.up.railway.app/health` retornou HTTP 200 `{"status":"ok"}`.
- Smoke autenticado no endpoint publicado: `GET /api/v1/members/onboarding-scoreboard` retornou HTTP 200 com 91 itens em 2.23s ponta a ponta.

## Hotfix 2026-05-27 - Central Cordex outcome antes de preparacao
- Sintoma no piloto: clicar `Concluir` ou outcomes rapidos em recomendacao IA ainda em `do_now` exibia `Recommendation precisa estar aprovada antes de registrar outcome.`
- Diagnostico: o backend bloqueava corretamente o registro de resultado antes da preparacao/aprovacao, mas `WorkExecutionView` mantinha os botoes de outcome ativos cedo demais.
- Correcao: recomendacoes `ai_triage` com `state != awaiting_outcome` agora mostram instrucao para comecar execucao primeiro e deixam `Concluir`/outcomes desabilitados.
- Defesa adicional: `recordOutcome` tambem bloqueia a chamada se algum botao for acionado antes da preparacao.
- `cd saas-frontend && npm.cmd test -- --run src/test/AITriageInboxPage.test.tsx`: 7 passed.
- `cd saas-frontend && npm.cmd run lint`: passou.
- `cd saas-frontend && npm.cmd run build`: passou.
- `specify check`: passou.
- Deploy frontend Vercel: deployment `dpl_AEWyBRFvPfppoQQbaxfT6ziHziSx`, alias `https://saas-frontend-pearl.vercel.app`.
- Smoke do piloto: `/tasks` retornou HTTP 200 e asset `WorkExecutionView-ZTdeV3ho.js` contem `Para registrar resultado` e `Comecar execucao`.
